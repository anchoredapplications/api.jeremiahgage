version: '3'
services:
  node-express-docker:
    environment:
      - NODE_ENV=production
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/usr/app
    container_name: node-express-docker
    expose:
      - "80" 
    ports:
      - "80:80"
    command: npm run build